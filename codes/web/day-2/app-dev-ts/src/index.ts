/*
var addRes = add(10, 20)
console.log(addRes)

var subRes = subtract(10, 5)
console.log(subRes)

var multiRes = multiply(10, 2)
console.log(multiRes)

calculate(add, 10, 20)
calculate(subtract, 10, 2)
calculate(multiply, 10, 2)
calculate(divide, 10, 2)
*/

// var btnObj = document.getElementById('btnCalculate')
// btnObj.addEventListener('click', doCalculate)

function doCalculate() {
    var txtFirstObj = document.getElementById('txtFirst')
    var txtSecondObj = document.getElementById('txtSecond')

    //Gets a collection of HTML element objects based on the value of the NAME or ID attribute.
    var allRadioButtons = document.getElementsByName('calcuationChoice')

    for (var i = 0; i < allRadioButtons.length; i++) {
       
    }

}