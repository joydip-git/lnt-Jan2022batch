using System;
using System.Collections.Generic;
using System.Text;

namespace CodilityCheck
{
    /*
    class Solution
    {
        public bool DoesHaveZero(string binaryValue)
        {
            bool doesExist = false;
            foreach (char ch in binaryValue)
            {
                if (ch == '0')
                {
                    doesExist = true;
                }
            }
            return doesExist;
        }
        public int solution(int N)
        {
            string binary = Convert.ToString(N, 2);
            if (DoesHaveZero(binary))
            {
                return 0;
            }
            else
                return 0;
        }
    }
    */
}
